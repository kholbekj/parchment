# About

Parchment is a minimal markdown navigation library.

## Features

- Intercepts `.md` link clicks
- Loads content via fetch (or custom resolver)
- Parses with marked.js (or custom parser)
- Renders to target element
- Handles browser history

Go back to [hello](hello.md).
